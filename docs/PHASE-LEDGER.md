# Phase ledger

Status of each phase in `BUILD_PLAN.md` section 15. **Live** = built and covered by automated
tests. **Scaffolded** = the contract (routes, job handler, workflow, screen) exists with a
stand-in so the app runs end to end; the real behaviour is the phase's remaining work.
**Not yet proven** = only a person with the test accounts can confirm it.

| Phase | Status | Notes |
| --- | --- | --- |
| 0. Proof tests | Not yet proven | Needs a person: Buffer API test post to test TikTok / IG / YouTube; OpenShorts on a real sample in Actions. Fakes cover the shapes. |
| 1. Foundation + Dump | Live | Repo, one-Worker deploy, email-code login, D1 schema (full data model), chunked resumable R2 uploads, two-door Dump with notes + Dump button, Home, phone layout, Connect (Buffer key check + channels), Settings + Health. |
| 2. Client Brain | Live (fakes) | Upload docs → extract job → draft profile → edit → lock → rollback; fake extract returns the real text. Not yet proven: OCR on a real scanned PDF in Actions; the OpenRouter free model returning a valid profile. |
| 3. Research Brief | Live (fakes) | Refresh → research job → cited brief (truth rule enforced on every store and read) → approve; Meta / Google OAuth start + callback (fake sign-in); section 6 gate on Dump. Not yet proven: Instagram Login with a real Meta app + Professional test account (metric names `views`, `ig_reels_avg_watch_time`); Google OAuth with the project "In production" + YouTube Analytics `averageViewDuration`; Firecrawl search credits; the free model returning a valid brief. Section 6 brief crons: see their row. |
| 4. Editing engine | Live (fakes) | ffmpeg + MediaPipe cutter in `jobs/cut.py` (the plan's own fallback; OpenShorts not used), moment choice, captions, recipes; the fake cutter runs a dump end to end. Not yet proven: a real dump from her phone through Actions → R2 → callback (needs the R2 + job secrets); the free model's moment choices on real footage; speed on the Actions CPU for a long dump (local: 60 s source → 6 clips in ~35 s on 6 cores). |
| 5. Review | Live (fakes) | Approve, reject with a reason, delete with a confirm, edit a caption, hidden clips; phone + desktop. One caption per clip (per-platform copy is a later column read by posting). |
| 6. Calendar + Buffer | Live (fakes) | Fill at caps (10 / 7 / 5), moves refused over the cap, take off, hourly sync loads Buffer, posts read back with links, a failed post retried twice then emailed, a channel flip → one email; Buffer real + fake client. Not yet proven with a live key: video-by-URL `AssetInput`, a YouTube title in `PostInputMetaData`, the Service enum's casing, the `PostId` / `ChannelId` scalars, the `posts(...)` queue-count query; no real post has gone out. |
| 7. Emails + Health | Live (fakes) | All six email kinds fire from the phase that owns the event; health rows (Buffer + one light per channel, clip cutting, email, job runner, stats) with fix guides; "Check everything now" rewrites them; the weekly recap lists due follow-ups. Not yet proven: Resend from her own domain (defaults to the onboarding sender). |
| 8. Learning loop | Live (fakes) | Metrics job (Instagram / YouTube via API, TikTok via the Studio export), `platform_videos`, learned posting slots once a platform has enough results, Stats screen, the expired-token failure shape. Not yet proven: real TikTok Studio export headers (the parser accepts the aliases listed in `tiktokImport.ts`); real Meta / Google metric names. |
| 9. Recycle + polish | Scaffolded | Cooldown + ranking unit-tested in `worker/domain/cooldown.ts`; recycle fields on Dump live. |
| 10. Brand deals | Live (fakes) | Brand finder job (public business contacts only, off-limits brands dropped), brand cards, pitch drafts with the Gmail link, mark sent → day-5 / day-12 follow-ups on Home and in the weekly recap, deal tracker, deliverables on won deals, TikTok One eligibility, public media kit. Not yet proven: the finder against live Firecrawl / OpenRouter / Hunter and contact quality on real brand sites; the Gmail compose link in her phone's Gmail app (desktop web verified). |
| 11. Voice | Live (fakes) | Behind the feature switch: sample + consent, Chatterbox voice job, narration on clips; the fake job runs end to end. Not yet proven: Chatterbox speed and quality on the Actions CPU runner (the 90-minute workflow timeout is a fault detector). |
| Section 6 brief crons | Live (fakes) | Monthly refresh: the daily lane, on the 1st (retrying the 2nd and 3rd if nothing started), dispatches the research job for a fresh draft; the next hourly run after it lands emails "New brief draft ready" once (`emails_sent.kind = 'brief_ready'`, migration 0005). Nothing waits on the owner: the monthly refresh does not stop for approval, it emails and the approved brief stays live. Weekly adjustment: the Monday lane rewrites only the live brief's `her_week` her-data claims from the last 7 days of `platform_videos` + `metrics`, stamps `adjusted_at`, never changes approval, never touches a web or upload claim. No new cron expression. Health rows `Monthly brief refresh` / `Weekly brief adjustment`. Tests: `tests/unit/brief-crons.test.ts` (pure rules + both lanes on the real schema). Not yet proven: a real monthly run end to end (needs the job runner secrets, see Staging). |
| Staging | Live (real) | `env.staging`: its own Worker, D1, R2, FAKE_SERVICES=0, the owner's login; `npm run deploy:staging`; `validate:envs` keeps it production's twin; job dispatch carries `env` so Actions picks the staging bucket and `JOB_SHARED_SECRET_STAGING`. Named stops and the Phase 0 live checklist are in RUNBOOK "Staging". |
| Hallmark design pass (brand-preserve) | Landed 6f2b297 | PR #11: A Sheila Bruce Affair tokens kept, every screen restyled; before/after captures and audit in `docs/design/`. Deployed to production and staging. |
| 12. Hardening + handoff | Live (fakes) | Help center: 42 guides with real steps, `?` on every screen, first-run tour, screenshot job + spec. Not yet proven: the screenshot PR step in Actions (the repo setting that lets Actions open PRs is on). The handoff checklist is still open. |

## Not yet proven (needs a person)

- A real Buffer key: the GraphQL queries in `worker/services/buffer.ts` are pinned against the
  documented schema, not a live account.
- Resend from her own domain (defaults to the onboarding sender).
- OpenShorts speed on the Actions CPU runner.
