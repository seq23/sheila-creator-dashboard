# Phase ledger

Status of each phase in `BUILD_PLAN.md` section 15. **Live** = built and covered by automated
tests. **Scaffolded** = the contract (routes, job handler, workflow, screen) exists with a
stand-in so the app runs end to end; the real behaviour is the phase's remaining work.
**Not yet proven** = only a person with the test accounts can confirm it.

| Phase | Status | Notes |
| --- | --- | --- |
| 0. Proof tests | Proven live 2026-09-26 | Staging, real accounts (`docs/PHASE0.md`, suite `tests/live`, `npm run e2e:live`). Real posts through Buffer to the three test channels, read back as Posted with links: TikTok `…/video/7689660837077847309`, Instagram `/reel/DdvCgMMFRhk/`, YouTube `/shorts/lFDhkdWMj0w`. The cutter on real footage in Actions (245 s TikTok-style source → 28 clips at 1080×1920). 12 bugs found and fixed (#16–#29). |
| 1. Foundation + Dump | Live | Repo, one-Worker deploy, email-code login, D1 schema (full data model), chunked resumable R2 uploads, two-door Dump with notes + Dump button, Home, phone layout, Connect (Buffer key check + channels), Settings + Health. |
| 2. Client Brain | Proven live 2026-09-26 | Upload docs → extract job → draft profile → edit → lock → rollback. Proven live: a 2-page PDF and an image-only scanned page read in Actions (OCR: 1,165 chars), the free model drafting a valid profile after #18 (answers cut off for length are asked again with more room). |
| 3. Research Brief | Live (fakes) | Refresh → research job → cited brief (truth rule enforced on every store and read) → approve; Meta / Google OAuth start + callback (fake sign-in); section 6 gate on Dump. Proven live 2026-09-26: Google OAuth on staging (Testing mode, test user) + Sync; the free model returning a labelled brief (57 claims) with web search off and saying so. Not yet proven: Instagram Login (being replaced by no-login Stats); Firecrawl search credits. Section 6 brief crons: see their row. |
| 4. Editing engine | Live (fakes) | ffmpeg + MediaPipe cutter in `jobs/cut.py` (the plan's own fallback; OpenShorts not used), moment choice, captions, recipes; the fake cutter runs a dump end to end. Proven live 2026-09-26: real footage through Actions → Worker → callback (245 s source → 28 clips in ~10 min; 66 s → 7; 44 s → 5), clips-ready email, playable 1080×1920 in Review; another creator's watermark held off the calendar (#20). Not yet proven: a dump from her own phone. |
| 5. Review | Live (fakes) | Approve, reject with a reason, delete with a confirm, edit a caption, hidden clips; phone + desktop. One caption per clip (per-platform copy is a later column read by posting). |
| 6. Calendar + Buffer | Live (fakes) | Fill at caps (10 / 7 / 5), moves refused over the cap, take off, hourly sync loads Buffer, posts read back with links, a failed post retried twice then emailed, a channel flip → one email; Buffer real + fake client. Proven live 2026-09-26 with the throwaway Buffer account: video-by-URL assets, TikTok / Instagram (reel) / YouTube (title + category) posts published and read back as Posted with links (#23 fixed the Instagram/YouTube metadata and the `error { … }` read-back); Fill within caps, move, take off; desktop week readable (#26). Idle Buffer spend measured: 20 requests/day (budget 25). |
| 7. Emails + Health | Live (fakes) | All six email kinds fire from the phase that owns the event; health rows (Buffer + one light per channel, clip cutting, email, job runner, stats) with fix guides; "Check everything now" rewrites them; the weekly recap lists due follow-ups. Proven live 2026-09-26: a revoked Buffer key → red light + exactly one "Buffer needs you" email; reconnect → green; "Check everything now" (sidebar refreshes at once, #16/#17). Not yet proven: Resend from her own domain (defaults to the onboarding sender). |
| 8. Learning loop | Live (fakes) | No-login stats first (row "Stats: no-login"); metrics job (Instagram / YouTube via the optional sign-ins, TikTok via the Studio export), `platform_videos`, learned posting slots once a platform has enough results, Stats screen, the expired-token failure shape. Real TikTok Studio export headers proven 25 Sep 2026 (the zip, `Content.csv`). Not yet proven: real Meta / Google metric names (optional sign-ins). |
| 9. Recycle + polish | Scaffolded | Cooldown + ranking unit-tested in `worker/domain/cooldown.ts`; recycle fields on Dump live. |
| 9b. Full video for YouTube | Live (fakes), 26 Sep 2026 | Third Dump door: one video posted whole (no cutter, no 9:16; validator `full-video-uncut`), job `fullvideo` (migration 0015), Review thumbnails / privacy / chapters / tags, one a week on the Calendar, Buffer post with her privacy, Finish in YouTube Studio, Upload it yourself fallback, storage rule (7 days after posting, 14 days unapproved). Tests `tests/unit/full-video.test.ts`, `jobs/tests/test_fullvideo.py`, `tests/e2e/youtube-full.spec.ts`. RUNBOOK "A full video for YouTube". |
| 10. Brand deals | Live (fakes), overhauled 26 Sep 2026 | Hostile review first (`docs/reviews/2026-09-25-mediakit-deals.md`, every row fixed) and the talent-manager view (`docs/reviews/agency-pov.md`). **Media kit**: one editor screen, autosaved draft, Preview, Publish = a dated version (the public link serves only the newest published one; drafts never public), old link names forward, view counter, Kit check with one-tap fixes; cover, handles, figures live from Stats each with its as-of date and source (nothing unverifiable; self-reported numbers marked), pillars, series, 3–6 clip showcase (kit clips keep their media link past the 30-day expiry), past collaborations (logos, from paid deals automatically), packages with public "starting at" / rates on request and private floor/target, add-on terms, testimonials, contact, QR code (own encoder, OpenCV-decoded when built), share-preview tags, `/kit/:slug/print` two Letter pages (migration 0009). **Deals**: money strip; "Do this next" (one next action per deal, overdue first) on an 11-stage pipeline (find contact → pitch → follow-up → negotiating → agreed → delivering → invoiced → paid → done, declined/lost with a reason); "Brands to pitch this week" ranked by budget signal × fit × reachability with the arithmetic and a source on every claim, unproven below the line; 18 email scenarios (starter + AI, tone, length, three subjects, "before you send", never an invented dollar figure), inbound paste → terms "from their email", red flags, verdict; rate helper from her own deals or a named 2026 benchmark or "no benchmark"; counter calculator, add-on math, lever scripts; deal memo, contract checklist, delivery checklist, day-7 results from Stats, printable invoice, payment reminder, rebook at 30 days; follow-ups day 5/12/19 then stop; Get listed here (14 marketplaces, `docs/BRAND-SOURCES.md`); daily light brand refresh + Monday full run; the finder needs no web key (jobs/common.py `Web`: Firecrawl if connected, else DuckDuckGo search + Jina reader). Guards: validators `mediakit-deals`, `web-research-optional`; tests `tests/unit/{ratecard,emails,offers,kit,deals-pipeline}.test.ts`, `jobs/tests/test_web.py`, `tests/e2e/mediakit-deals.spec.ts`. Proven on staging 26 Sep 2026 (real GitHub runner, real brand sites): with Firecrawl connected the finder found 9 new brands (money signals and "why" lines sourced, one local business); with Firecrawl disconnected it made 0 Firecrawl calls, got 44 search hits through the keyless chain (DuckDuckGo answers runners with 202, so the job moves on to DuckDuckGo through the Jina reader, PR #35) and found 8 new brands and agencies, light green. Not yet proven: the free model's drafts and offer reading on real brand emails; the Gmail compose link in her phone's Gmail app. |
| 11. Voice | Live (fakes) + ElevenLabs premium | Behind the feature switch: sample + consent, Chatterbox voice job, narration on clips; the fake job runs end to end. 25 Sep 2026: two engines, built-in (free, Chatterbox on the runner) and ElevenLabs (premium, her own account, called from the Worker): Connect card "Voice · ElevenLabs (premium)" (tier, characters used/limit, cloning on the plan), clone on sample save, premium narration in seconds with fallback to built-in on a refused key / used-up credits / any error, "Use premium voice when connected" switch, Premium/Built-in tag per narration, `Voice · ElevenLabs` light (migration 0006). Phone-first 5-step recording setup with the ~3-minute read-aloud script on screen (iPhone Safari recorder with timer, level meter, play-back; Voice Memos upload; at least 1 minute). Tests: `tests/unit/voice-engine.test.ts`, `tests/e2e/voice-engines.spec.ts`; validators `voice-engines`, `voice-script`. 26 Sep 2026 (owner: "nothing should be hidden - she can use it if she chooses, nothing switched off"): the screen is "Voice overs", always in the menu; Home card "Your voice overs"; every Settings feature on by default (migration 0010); switch "Voice overs on clips" on the screen and in Settings; validator `nothing-hidden`. Proven live 2026-09-26: the built-in voice end to end on staging (#23 fixed the Chatterbox TypeError): 64 s consent sample → voice job 4.4 min → 9.1 s narration plays → attached to a clip → Delete my voice. Not yet proven: a real ElevenLabs clone + narration (staging is connected with a real key on a plan without instant voice cloning, so the real proof so far is the no-cloning path). 26 Sep 2026: Automatic voice overs (#42, migration 0014): clips with no talking voiced in her voice in one voice job run per dump, AI label on posts via Buffer isAiGenerated, Review Remove / Redo (Edit the script); Dump chip "Voice over" + notes + Review Add voice over; validator `auto-voice-silent-only`; tests `tests/unit/auto-voice.test.ts`, `jobs/tests/test_voice_batch.py`, `tests/e2e/auto-voice.spec.ts`. |
| Section 6 brief crons | Live (fakes) | Monthly refresh: the daily lane, on the 1st (retrying the 2nd and 3rd if nothing started), dispatches the research job for a fresh draft; the next hourly run after it lands emails "New brief draft ready" once (`emails_sent.kind = 'brief_ready'`, migration 0005). Nothing waits on the owner: the monthly refresh does not stop for approval, it emails and the approved brief stays live. Weekly adjustment: the Monday lane rewrites only the live brief's `her_week` her-data claims from the last 7 days of `platform_videos` + `metrics`, stamps `adjusted_at`, never changes approval, never touches a web or upload claim. No new cron expression. Health rows `Monthly brief refresh` / `Weekly brief adjustment`. Tests: `tests/unit/brief-crons.test.ts` (pure rules + both lanes on the real schema). Not yet proven: a real monthly run end to end (needs OpenRouter connected on staging). |
| Staging | Live (real) | `env.staging`: its own Worker, D1, R2, FAKE_SERVICES=0; `npm run deploy:staging`; `validate:envs` keeps it production's twin. Email real: Resend delivers login codes and every email to the Resend owner's address `sequoia@westpeek.ventures` (staging's OWNER_EMAIL); an agent reads a code with `scripts/staging-login-code.mjs`. Dispatch real: `GITHUB_DISPATCH_TOKEN` is Sequoia's own GitHub token (production too). Jobs real via Worker: jobs hold no storage keys, they stream inputs with a signed GET and write outputs through the signed chunked upload, limited to their own folders (`worker/lib/jobStorage.ts`, guard `jobs-no-direct-storage`). Named stops: none. The Phase 0 live checklist is in RUNBOOK "Staging". |
| No-login production + rename | Live | The owner's instruction (26 Sep 2026): no login on production, URL https://sheilastudio.seq-taylor.workers.dev (Worker `sheilastudio`, same D1 and R2). `AUTH_MODE` "open" on production: every request is the owner (OWNER_EMAIL, created on first hit), `/api/auth/*` 404, `/login` → Home, Help hides the "Log in" guide. Staging, local and e2e keep the email code (`AUTH_MODE` "code"). With open mode anyone who has the URL is the owner; that is by her choice; switching back is `AUTH_MODE: "code"` and a deploy. Guards: `tests/unit/auth-mode.test.ts`, `envs-match` (production open, staging code, no `/login` link in `app/`), `npm run e2e:open` (CI job `e2e-open`), deploy smoke `scripts/auth-mode-smoke.sh`. |
| Looks (editing variety) | Live (fakes) + selftest | Owner, 25 Sep 2026: "not enough variety". 12 Looks (6 single: Clean, Bold hook, Karaoke captions, Brand card, Cinematic, Reaction inset; 6 grids: Split moment, Side by side, Grid of four/six/eight, Hero and strip) in `jobs/looks.json` + `worker/domain/looks.ts`, rendered by `jobs/looks.py` (ffmpeg + libass + MediaPipe, no paid service). Each dump rotates through her enabled Looks (singles and grids 2:1); clips record `look`, `parts`, `layout` (migration 0008). Review: Look chip, Change look with previews, grid cell picker (cells from this dump, her approved clips, closer zooms; voice cell), re-render keeps the old version until the new one is ready. Settings > Editing: Looks in the mix, captions, end card, music bed from My music (her own songs only). Watermark check names the creator (8 frames, second OCR pass, handles counted across frames; fixture from the staging videos). Tests: `tests/unit/looks.test.ts`, `tests/e2e/looks.spec.ts`, selftest renders every Look; validator `looks`. Not yet proven until the staging run: render time of 8-cell grids on a long real dump on the Actions CPU. |
| Stats: no-login | Live (real on staging) | Owner decision 25 Sep 2026: production Stats never needs a Google or Meta sign-in. YouTube public numbers with `YOUTUBE_API_KEY` (channel from Buffer's serviceId or typed once on Stats); Instagram public numbers when Instagram answers, otherwise (measured: 401 / 302 → login from the Worker) the always-present "Your Instagram numbers" form with a 3-step guide and "Remind me monthly" (Monday recap line); TikTok export accepted as the zip TikTok hands her, post time from the video id when the export omits the year. Sign-ins stay visible as optional extra detail. Update numbers never refused; daily + Monday lanes refresh; the metrics job only with a sign-in. Proven on staging: YouTube UC7O1lQikHSc77s7gNnj9htQ 1 subscriber / 0 views / 0 videos; the real TikTok zip → 5 videos. Guards: `tests/unit/stats-no-login.test.ts`, e2e "stats with no sign-in", validator `stats-no-login`. RUNBOOK "Stats: no-login". |
| Editors (CapCut hand-back + connected editors) | Live (fakes) | Owner, 25 Sep 2026: "connect her own CapCut and use that in lieu of our open source alt… any other premium popular influencer software (only the most popular)". Research per tool in `docs/EDITORS.md`. CapCut and InShot have no API: Edit in CapCut on every clip (share/save, edit, Replace with my edit: header check in the Worker, import mode measures again, loudness, cover, swap, "Edited in CapCut"). Connected editors with a verified self-serve API: Opus Clip, Vizard, Klap, Submagic, Descript (Connect rows with Check key, Disconnect, health light; Who edits per job; dump cut, captions, polish; every failure falls back to built-in). Not built, decided: Canva (designs, no video editing), Captions/Mirage (endpoints unpublished), VEED and Adobe Express (no self-serve API). Tests: `tests/unit/editors.test.ts`, `tests/e2e/editors.spec.ts`, selftest import mode. Not yet proven: every real vendor call (no key in the vault). |
| Steering a dump (creative control) | Live (fakes) + selftest | Owner, 26 Sep 2026. Hostile review `docs/CREATIVE-CONTROL-REVIEW.md`. Which-videos cards (nothing preselected, hint bubbles, the Dump button names the choice); Surprise me with What we tried; chips (look incl. grids, music, pace, length, how many, captions, platforms); dump and per-video notes read by rules (+ free AI when connected) into the same controls plus must include / leave out, shown before Dump; anything not followed is listed on the dump with why; Review Change music and Try another version (migration 0013). Tests: `tests/unit/steer.test.ts` (13 note fixtures + spec honoring on the real schema), `tests/e2e/steer.spec.ts`, selftest `check_steer`; validator `steer-honored`. Not yet proven: the free AI's reading of notes the rules miss (staging has no OpenRouter key). |
| Day 358 (archive, Tidy up, storage budget, Home caps) | Live (fakes), 26 Sep 2026 | Owner: "think about day 358… dismiss dumps that are old… cards that are stacking up" + "account for space and storage". Hostile review first on a year of demo data (`docs/reviews/2026-09-26-day-358.md`, `scripts/seed-year.mjs`, before/after `docs/design/day-358/`). Archive + Undo + Show archived for dumps, deals, briefs, voice overs; Home cards dismiss with Undo; Home capped (`HOME_CAPS`, fits an iPhone 13 screen); Tidy up (on by default); storage meter (every file), posted clip files 30 d unless in the kit, drafts 60 d with the Home warning first and Keep, 9 GB hard budget; paged lists with true counts (Dump, Review, Calendar → History, Voice overs, Deals, kit versions); dates carry the year. Migration 0016. Guards: validators `home-caps`, `tidy-warns-first`, `lists-paged`, `seed-year-local-only`; `tests/unit/day-358.test.ts`, `tests/e2e/day-358.spec.ts`; guides `archive-and-undo`, `storage-and-tidy`, `storage-almost-full`. RUNBOOK "Day 358". |
| YouTube direct (full videos to her channel) | Proven live 2026-09-26 (staging) | Owner decision 26 Sep 2026. Public /privacy and /terms (#53, Google Branding). Connect YouTube (full videos) on Connect (upload + readonly, incremental, encrypted token), the hourly lane uploads approved full videos within 7 days of their slot via the ytupload job (resumable, short-lived token from the Worker), read back with videos.list, Calendar moves → publishAt, taken off → private and kept, 3 a day, every failure named (quota, revoked, publish time, unverified thumbnail, kept private, interrupted). Migration 0017. Guards: validator `youtube-direct`, `tests/unit/youtube-direct.test.ts`, `jobs/tests/test_ytupload.py`, `tests/e2e/youtube-direct.spec.ts`; RUNBOOK "YouTube: full videos straight to her channel". Staging proof 26 Sep 2026 (channel "Sequoia Taylor", synthetic 180 s 1280x720 TEST video, real GitHub runner, 33 MB in 4 chunks, 0 resumes): Private → video DQlRCUwW17c, videos.list {privacyStatus private, publishAt null, uploadStatus uploaded, failureReason null, rejectionReason null}, post Posted; Public a week out → 3bHoCHeBTgw, videos.list {privacyStatus private, publishAt 2026-10-03T11:55:33Z, uploadStatus uploaded} (the upload is NOT locked private: publishAt kept). thumbnails.set 403 on both → "Verify your channel's phone number" note, yellow. Calendar (after the force-ssl reconnect, 3bHoCHeBTgw, each change checked against videos.update's answer and confirmed by videos.list 3 minutes later): made private → {private, publishAt null}; put back 2026-10-04T15:00Z → {private, 2026-10-04T15:00:00Z}; moved → {private, 2026-10-05T16:00:00Z}; taken off → {private, publishAt null}, kept. uploadStatus processed, no failure/rejection reason throughout. Found and fixed on the way: youtube.readonly cannot update (#58, force-ssl), take-offs not read back (#59), videos.list lags an update by seconds (#60). Nothing went public. |
| Hallmark design pass (brand-preserve) | Landed 6f2b297 | PR #11: A Sheila Bruce Affair tokens kept, every screen restyled; before/after captures and audit in `docs/design/`. Deployed to production and staging. |
| 12. Hardening + handoff | Live (fakes) | Help center overhauled 26 Sep 2026 (`docs/HELP-REVIEW.md`, owner: "the help section has the same screenshot"): 75 guides (12 new: posting, health lights, voice overs, stats, switches, rates, brand finder, deal stages, deal memo, marketplaces, steering a dump, another version / music), one picture per step of exactly that step, 666 pictures all unique (was 560 with 260 unique: the job circled the page heading when a step had no target). Steps on other sites are labelled illustrations (`tests/e2e/help-mocks.ts`); each step sets its own demo state (`seed-help-extra.sql`, `seed-help-lights.sql`); a missing target or a duplicate picture fails the job; validator `help-pictures`; search answers her questions (`tests/unit/help-guides.test.ts`); tour follows the menu (8 stops, "Show me how"). CI: e2e.yml job `help-screenshots`. Proven live 2026-09-26: the screenshot job opens its PR and approves its own merge-gate run (#29). The handoff checklist is still open. |

## Not yet proven (needs a person)

- Resend from her own domain (defaults to the onboarding sender).
- A dump from her own phone (the live proof used downloaded and synthetic videos).
- Instagram sign-in stats (being replaced by no-login Stats, owner decision 25 Sep 2026).
